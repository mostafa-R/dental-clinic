/**
 * This file is intentionally empty.
 *
 * Clinic roles are 100% dynamic — they are stored in the Role collection
 * and created/managed by clinic admins via the RBAC UI.
 *
 * Site admin roles are defined as a static enum in:
 *   modules/site/admin/admin.model.js  →  SITE_ROLES
 */
export const ROLES = [];
